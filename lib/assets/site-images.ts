import { DEFAULT_HERO_BACKGROUND_PATH } from "@/lib/site-settings/defaults";

/** OG·조직 로고 등 — Hero 배경 fallback과 동일 */
export const HERO_IMAGE_PATH = DEFAULT_HERO_BACKGROUND_PATH;

export const SITE_OG_IMAGE_PATH = HERO_IMAGE_PATH;

export const ORGANIZATION_LOGO_PATH = HERO_IMAGE_PATH;

export { DEFAULT_HERO_BACKGROUND_PATH };
